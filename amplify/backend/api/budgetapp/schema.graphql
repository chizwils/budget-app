type Payments {
  id: ID
  amountPaid: Float
  createdAt: String
  updatedAt: String
}

type Debt @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  createdAt: String
  currentAmountOwed: Float
  initialAmountOwed: Float
  isPaidOf: Boolean
  payments: [Payments]
}
 